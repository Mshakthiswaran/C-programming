#include <stdio.h>
#include <stdlib.h>

int main()
{
    // ARITHMETIC OPERATER
    int x = 10,y=2,z=0;
    z=x+y;
   /* z=x-y;
    z=x*y;
    z=x/y;
    z=x%y;
    x++;
    x--;*/
     printf("the addition is %d",z);
    return 0;
}
